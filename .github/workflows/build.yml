name: Build Talos

on:
  push:
    branches: [main]
    tags: ['*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag for the built version'
        required: true

jobs:
  build:
    name: Build Talos
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Setup
        id: setup
        env:
          TAG: ${{ inputs.tag || github.ref_name }}
        run: echo "tag=v${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Docker Buildx (with insecure entitlement)
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
        #with:
        #  buildkitd-flags: --allow-insecure-entitlement security.insecure

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply Kernel Patch
        working-directory: ./pkgs
        run: >
          sed -i '/^\s*LLVM: 1/d' kernel/build/pkg.yaml

      - name: Build and push kernel (pkgs submodule)
        env:
          REGISTRY: ghcr.io
          USERNAME: ${{ github.repository_owner }}
          PUSH: "true"
          TAG: ${{ steps.setup.outputs.tag }}
          PLATFORM: linux/amd64
        working-directory: ./pkgs
        run: make kernel

      - name: Build Talos
        working-directory: ./talos
        env:
          REGISTRY: ghcr.io
          USERNAME: ${{ github.repository_owner }}
          PUSH: "true"
          TAG: ${{ steps.setup.outputs.tag }}
          PKG_KERNEL: ghcr.io/${{ github.repository_owner }}/kernel:${{ steps.setup.outputs.tag }}
          INSTALLER_ARCH: amd64
          PLATFORM: linux/amd64
        run: make kernel initramfs imager installer-base
